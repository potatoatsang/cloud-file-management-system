# 雲端檔案管理系統 — Cursor 開發規則

規格以根目錄 `REQUIREMENTS.md` 為準。範圍與開發順序只寫在本檔與 `REQUIREMENTS.md`（SDD）。開發採 **必備功能的 Domain TDD**（先寫 xUnit 再實作至全綠）；**進階功能一旦列入實作，也必須先寫 xUnit 再實作**。

## 1. 技術選型

- 執行環境 / 語言：.NET 8、C# 12
- 專案：`FileSystem.Domain`、`FileSystem.Console`、`FileSystem.Web`、`FileSystem.Tests`
- 物件樹：行程內記憶體，由 `SampleTreeFactory` 建立題目範例結構
- 測試：xUnit + FluentAssertions；Domain 使用真實物件實例
- HTTP：ASP.NET Core Minimal API（最後加分階段）
- 前端：Vue 3 Composition API（`<script setup>`）、TypeScript、Tailwind CSS、Axios（最後加分階段）
- 文件：先繪製目標 `docs/class-diagram.md`、`docs/er-model.md`（Mermaid，含功能四）；`README.md` 於功能一～四實作之後撰寫

## 2. 分層

- `FileSystem.Domain`：零 NuGet。遍歷記錄寫入 Visitor 的 Trail 集合；由 Console 或 Web 負責顯示。任一目錄可 Accept；總容量與任意副檔名搜尋皆為 Domain 能力。`PrintVisitor` 禁止寫死預期字串。
- `FileSystem.Console`：固定官方 Demo 劇本，依序走真實 Visitor 印出結果。不提供互動 CLI。面試官可修改 `Program.cs` 或 `SampleTreeFactory.cs` 後重跑以驗證。
- `FileSystem.Web`：最後加分；Minimal API 暴露已存在的 Domain。以 `wwwroot` 託管 Vue 建置產出。Use case / DTO 可放在 Web。
- `FileSystem.Tests`：只測試 Domain（及之後與 Domain 行為等價的服務）。斷言使用 FluentAssertions。

依賴方向：Console / Web / Tests → Domain。前端只透過 HTTP 取得結果，不在瀏覽器重算容量或搜尋。

## 3. 領域不變量

- `FileSystemNode`（abstract）：`Guid Id`、`string Name`、`Directory? Parent`、`DateTime CreatedAt`、標籤集合
- `Directory`：子節點集合；Root 的 `Parent` 為 null
- `File`（abstract）：`long SizeBytes`、`string Extension`；Parent 必須是 Directory
- `WordFile`：`int PageCount`
- `ImageFile`：`int Width`、`int Height`
- `TextFile`：`string Encoding`
- 檔案沒有 Children；樹不可有環
- 容量內部單位為 bytes；**1 KB = 1024 bytes**；顯示再格式化為 B / KB / MB
- 標籤僅限 `Urgent`、`Work`、`Personal`，可多選；進階階段必須有可呼叫的貼標／移除 API（不能只有 enum 與私有集合）
- `CalculateSize()`：File 回傳自身 bytes；Directory 回傳子樹 bytes 加總（供走訪累加）。對外「計算總容量」必須走 Visitor，結果為 `TotalSize`，才能產生 Trail

## 4. Seed

唯一準則為題目「客戶範例結構」，由 `SampleTreeFactory.Create()` 建立。樹狀 **Print 顯示名稱** 必須能對照題目 plaintext：含 `├──`、`└──` 與 `[目錄]`／`[子目錄]`（icon 可忽略）。

- 根目錄 (Root)
  - 專案文件 (Project_Docs)：需求規格書.docx（Word，15 頁，500KB）、系統架構圖.png（Image，1920x1080，2MB）
  - 個人筆記 (Personal_Notes)：待辦清單.txt（Text，UTF-8，1KB）、**顯示名** `2025備份 (Archive_2025)` [子目錄]／舊會議記錄.docx（Word，5 頁，200KB）
  - README.txt（Text，ASCII，500B）

同一 Archive 節點：Print 為中英並列 `2025備份 (Archive_2025)`；XML 目錄 tag 仍為 `<Archive_2025>`（對齊題目 XML 範例）。兩者都要滿足。

## 5. 設計模式

### Composite

檔案與目錄皆為 `FileSystemNode`。演算法以 Visitor 擴充。

### Visitor

`IFsVisitor`：`VisitDirectory`、`VisitWordFile`、`VisitImageFile`、`VisitTextFile`

- `PrintVisitor`：樹狀文字（真實走訪，非寫死字串）
- `CalculateSizeVisitor`：對外單一結果 `TotalSize`（含單位顯示字串）。不得並列 `TotalBytes` 與 `DisplaySize`。檔案子樹加總仍以 bytes 進行，再格式化為 `TotalSize`。
- `SearchVisitor`：任意副檔名（`.` 可有可無），結果為完整路徑
- `XmlExportVisitor`：題目指定 XML

每個 Visit 方法將 `Visiting: {parentName} -> {node.Name}` 追加到 Trail。Root 可用 `(root) -> 根目錄`。節點名用物件 `Name`。Console 在計算大小與搜尋後印出 Trail（逐步紀錄；不要求題目那種單行路徑，也不要求互動 CLI）。

XML：

- 目錄 tag：有中英並列時為 `中文_英文`（空白改 `_`），**例外**：Seed 之 `2025備份 (Archive_2025)` 的 tag 為 `Archive_2025`
- 檔案 tag：`主檔名_副檔名`
- 內文格式對齊題目（頁數 / 解析度 / 編碼 + 大小）

### 進階功能用到的模式

- Strategy：`ISortStrategy`（名稱、大小、副檔名），支援升冪與降冪
- Prototype：`FileSystemNode.Clone()` 深拷貝子樹並產生新 Id
- Command：`ICommand`（`Execute` / `Undo`）、`DeleteNodeCommand`、`CopyPasteNodeCommand`、`CommandHistory`（undo / redo stack）
- 建議另有 `SortCommand`、`TagCommand` 以便復原

進階驗收：Domain 測試 + Console 劇本（排序後印樹、刪除／貼上、標籤、undo／redo），不依賴 UI。

## 6. API

最後加分階段才實作。Visitor 類端點回傳資料與 `traverseLog`（字串清單）並列。建議：

- `GET /api/tree`
- `POST /api/visitors/size`
- `POST /api/visitors/search`
- `POST /api/visitors/xml`
- 進階已做時：delete / paste / sort / tag / undo / redo

啟動時 Seed；記憶體單例持有 Root。不改 Domain 演算法。

## 7. 前端（最後階段）

僅在目標圖、功能一～四與 README 完成後實作。組件：`FileTree.vue`、`VisitorPanel.vue`、`TraverseConsole.vue`；進階工具列。以 Axios 呼叫後端並渲染 DTO。

## 8. 測試

必備功能先寫再實作，以範例樹為 fixture：

- 結構與各檔 metadata 正確；樹狀輸出可對照 plaintext 樹枝與 `[目錄]`／`[子目錄]`
- Print 含 `2025備份 (Archive_2025)`；XML 含 `<Archive_2025>`
- Root 的 `TotalSize` 依 1024 換算且含單位顯示正確
- `.docx` 恰好兩條路徑：需求規格書、舊會議記錄
- XML 含 `<根目錄_Root>` 與範例節點
- Size / Search 的 Trail 為逐步 `Visiting: {parent} -> {node}`，非空且含拜訪過的目錄與檔名

進階功能一旦列入實作，必須先補 xUnit 再寫程式（排序、刪除／貼上、貼標／移除、undo／redo）。

## 9. 開發順序

SA 先行：先圖、再實作功能一～四對齊圖、再 README、最後 Web。

1. 規格（`REQUIREMENTS.md`、本檔）
2. 目標 UML／ER（含功能四：Visitor、Tag、Strategy、Command／History、Clone；ER 含標籤）
3. 功能一～三：Domain TDD → `FileSystem.Console` 官方劇本（對齊圖）
4. 功能四：排序 → 刪除／複製貼上 → 標籤 → Undo／Redo；先測再寫；Console 加劇本（對齊圖）
5. `README.md`（還原、測試、Console、設計與 pattern）
6. `FileSystem.Web` Minimal API（不改 Domain 演算法；必備 + 已做的進階端點）
7. Vue + Axios：FileTree → VisitorPanel → TraverseConsole → 進階工具列

硬性規則：

- 未完成步驟 2 前，不開始步驟 3–4 的新開發。
- 未完成步驟 3–4 前，不定稿 README、不開始步驟 6–7。
- 未完成步驟 2–4 前，不建立 Vue。
- Web 是最後加分，用來暴露已存在的 Domain。
- 實作與圖不一致時，先改圖再改碼。

## 10. Git

僅在使用者明確要求 commit 或 push 時執行。

- commit 前：`git status`、`git diff`
- 訊息：Conventional Commits，說明為繁體中文（例：`feat(domain): 實作目錄總容量 Visitor`）
- 只暫存相關原始碼與文件；忽略 `bin/`、`obj/`、`node_modules/` 等（見 `.gitignore`）
- 生命週期對應 commit：`docs` 規格 → `docs` UML／ER → `feat` 必備 → `feat` 進階 → `feat(web)` → `feat(frontend)`
- README 於進階之後、Web 之前以 `docs:` 提交
- 遠端 repo 名稱：`cloud-file-management-system`

## 11. 完成定義

- 目標 `docs/class-diagram.md`、`docs/er-model.md` 先於功能一～四存在且含功能四
- `dotnet test` 全綠（含已列入的進階測試）
- Console 官方劇本可 Demo 必備功能：樹狀結構（對照 plaintext）、`TotalSize` 與 Trail、搜尋 `.docx` 與 Trail、XML
- 進階若實作：Console 劇本可 Demo 排序後印樹、刪除／貼上、標籤、undo／redo
- README 於實作 1–4 之後說明執行方式與 pattern
- Web／Vue 為加分；若實作才適用：看得到樹、三個 Visitor 操作、畫面顯示後端 `traverseLog`
